<nav class="bg-purple-700 text-white px-6 py-4 shadow-lg">
  <div class="max-w-6xl mx-auto flex justify-between items-center">

    <h1 class="text-2xl font-bold">
      TaskFlow
    </h1>

    <div class="space-x-4">
      {{#if user.name}}
        <span class="hidden sm:inline-block text-sm">Hello, {{user.name}}</span>
        <a href="/logout" class="hover:text-gray-200 transition">Logout</a>
      {{else}}
        <a href="/login" class="hover:text-gray-200 transition">Login</a>
        <a href="/signup" class="hover:text-gray-200 transition">Signup</a>
      {{/if}}
    </div>

  </div>
</nav>